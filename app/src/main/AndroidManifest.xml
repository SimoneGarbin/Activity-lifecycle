<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="math.unipd.it.mp.lifecycle" >
    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="15" />
    <application android:label="@string/app_name"
        android:icon="@drawable/ic_launcher">

        <activity android:name="math.unipd.it.mp.lifecycle.ActivityA"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="math.unipd.it.mp.lifecycle.ActivityB" />

        <activity android:name="math.unipd.it.mp.lifecycle.ActivityC" />

        <activity android:name="math.unipd.it.mp.lifecycle.ActivityD" />


        <activity android:name="math.unipd.it.mp.lifecycle.DialogActivity"
            android:theme="@android:style/Theme.Dialog">
        </activity>

    </application>

</manifest>
